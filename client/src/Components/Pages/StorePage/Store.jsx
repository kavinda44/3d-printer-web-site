import React from 'react';
import "./StoreStyles.css"
import NavBar from '../../NavBar/NavBar';

const Store = () => {
    return (
        <div>
            <NavBar/>
        </div>
    );
}

export default Store;
